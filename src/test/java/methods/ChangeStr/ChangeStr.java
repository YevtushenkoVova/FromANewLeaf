package methods.ChangeStr;

public class ChangeStr {

    public static String[] changeStrElem(String str, String element) {
        String[] arr = str.split(element);
        for (String elem : arr) {
        }
        return arr;
    }
}
